class MCPServerService:
    """
    MCPServer 관련 비즈니스 로직 및 외부 repo 파싱 기능을 제공하는 서비스 클래스
    """

    @staticmethod
    def read_mcp_server_tool_list(github_link: str) -> list:
        """
        주어진 github_link에서 MCP 서버의 tool 목록을 추출한다.
        (실제 구현은 추후)
        """
        # TODO: github API 호출 및 파싱 로직 구현
        return []

    @staticmethod
    def read_mcp_server_resource_list(github_link: str) -> list:
        """
        주어진 github_link에서 MCP 서버의 resource 목록을 추출한다.
        (실제 구현은 추후)
        """
        # TODO: github API 호출 및 파싱 로직 구현
        return []
